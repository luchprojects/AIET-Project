"""
Compatibility module for main_window imports.

The current main_window implementation no longer requires helper symbols from
this module at runtime, but keeps the import for legacy compatibility.
"""

